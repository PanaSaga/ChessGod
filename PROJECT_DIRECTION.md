# PROJECT_DIRECTION.md — 작업 방향 정리

> 이 문서는 `~/Downloads/CLAUDE.md`(claude.ai 기획 논의 요약)와 실제 저장소(`C:\Project\ChessGod`, 브랜치 `OutGame_yw`) 상태를 대조 진단한 뒤, 앞으로의 작업 방향을 정리한 문서입니다.
> **아직 아무 코드/에셋도 수정하지 않았습니다.** 실제 작업은 매 단계 사전 승인 후 진행합니다.
>
> **2026-07-14 업데이트: 방향 전면 재설정.** 이전 버전 문서의 "진행도/완료 단계" 관련 내용은 전부 폐기합니다. 아무것도 완료된 것으로 가정하지 않고, **이미 만들어진 게임(인게임 로직) 위에 UI를 하이라키/스크립트 포함 전부 처음부터 새로 제작**합니다.

---

## 1. 현재 확정된 전제

- **인게임(체스 게임 로직)은 이미 만들어져 있음** — `Assets/Scripts/InGame/` 하위 (`GameManager.cs`, `SpawnManager.cs`, `ControlManager.cs`, `BoardViewManager.cs`, `BoardManager.cs`, `ChessPiece/*`, `View/*`) + `Assets/Scenes/InGame.unity`. 이 로직/씬은 건드리지 않는 것이 기본 전제이며, 손대야 할 경우 반드시 먼저 설명하고 승인받음.
- **UI는 전무한 상태로 간주하고 처음부터 제작.** 기존에 다른 브랜치(`origin/UITest` 등)에 있던 UI 스크립트나 "몇 단계까지 완료됐다"는 기록은 **참고하지 않고 전부 무시**. 재사용/포팅 없음.
- **하이라키(씬 GameObject 구조)도 새로 구성.** 기존 씬에 UI용 오브젝트가 있다면 그것도 새로 설계해서 채워 넣는 대상이지, 이어받는 대상이 아님.
- CLAUDE.md의 **기획 내용**(용어, 체스말 규칙, 업적 7종, 화면 구성 사양, 계약 인터페이스 아이디어 등)은 "무엇을 만들지"에 대한 참고 자료로는 유지하되, 이후 실제 설계 단계에서 사용자와 다시 확인하며 확정합니다 — 자동으로 그대로 구현 대상이 되는 것은 아닙니다.

---

## 2. 앞으로의 작업 규칙 (변경 없음)

- **직접 파일을 건드리기 전 반드시 승인받는다.** 코드/씬/에셋 수정은 먼저 무엇을 어떻게 바꿀지 설명하고 승인받은 후에만 진행.
- **수정 전 백업 파일을 만든다.** 기존 파일을 수정할 때는 수정 전 상태를 백업(타임스탬프 붙은 백업 경로 등)으로 남긴 뒤 작업. 완전히 새로 만드는 파일은 백업 대상은 아니지만 여전히 사전 승인 대상.
- 파괴적 git 작업(브랜치 삭제, force push, reset --hard 등)은 항상 별도 승인.

---

## 3. 다음 단계 — 확인 필요

처음부터 UI를 설계하려면 먼저 아래를 정해야 합니다:

1. **씬 구조**: 기존 `InGame.unity` 하나만 쓰고 그 안에 UI를 올릴지, 아니면 로비/갤러리 등 별도 씬을 새로 만들지 (CLAUDE.md 기획서는 BootScene/MainScene/GalleryScene/GameScene 4개 구조를 제안했었는데, 이 구조를 목표로 할지 재검토할지)
2. **범위**: 이번에 만들 UI 범위 — 인게임 HUD(라이프/점수/타이머 등)만 먼저 할지, 로비/업적/갤러리까지 포함할지
3. **데이터 연결 방식**: `GameManager.cs` 등 기존 인게임 스크립트에서 값을 어떻게 가져올지 — 계약 인터페이스를 새로 설계할지, 직접 참조로 간단히 시작할지

이 세 가지를 정하면 그에 맞춰 구체적 실행 계획(만들 스크립트/오브젝트 목록)을 다시 문서로 정리해서 보여드리겠습니다.

---

## 4. 참고 — Unity 작업 시 유의사항 (CLAUDE.md 원문에서 유지, 여전히 유효한 기술적 조언)

- Unity `OnClick()`은 파라미터 0~1개 함수만 인스펙터에 노출 → 2개 이상은 파라미터 없는 래퍼로 분리
- 스크립트/클래스 이름 변경 시 전체 프로젝트 재검색 필수 (참조 누락 시 CS0103)
- 동일 클래스명 중복 파일 주의 (CS0101)
- VS Code ".NET SDK 없음" 알림은 Unity 자체 컴파일과 무관
